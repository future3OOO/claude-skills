# PR #138 Fusion Program

Owner: this session (took ownership from the original #138 agent, operator-directed 2026-08-19).
Source of truth: `main` (post-#139, `6d57f1e`); this branch `fix/tdd-behavior-map-evidence` at the live PR head; conflict rule: main wins on doctrine, this program wins on TDD machinery shape.
Work class: implementation + refactor on an existing PR, then two follow-up PRs.

## Objective

Land #138 (Behavior Map as load-bearing TDD evidence) production-ready, absorb its
shallow adapter into the module that owns candidate identity, then ship the two
#137-fusion features on the absorbed shape.

## Scope

In: main-integration of #138; migration of its four straggler tests; the
absorption refactor (mapped TDD behavior into the owner of candidate/drift/run
validity); sourceRefs + design/advisor reconciliation; stage-aware behavioral
`fixed` enforcement resolved through the ledger.
Out: reassessment telemetry (prospective, needs its own occurrence case);
`greenEvidenceId` (measured 2026-08-19: each ledger cycle document already
carries top-level `behaviorId` plus its valid green run — an unambiguous
pointer exists; a cache field is added only if the stage-aware validator's
ledger walk measures as expensive); any second advisor phase, new table, or
parallel ledger; cycle quotas.

## Verified evidence base (2026-08-19)

- `git merge-tree --write-tree main d6cddae` exits 0: zero conflicts; sole
  shared file `skills/production-preflight/SKILL.md`, disjoint hunks.
- P1 correction of the original diagnosis: the visible 4 failures at d6cddae
  had THREE mechanisms — (a) two hook tests driving legacy free-form REDs
  against no-change maps (fixture stragglers); (b) a policy-gate substring
  drift; (c) a REAL production contradiction: commit 0872eff closed the
  production-edit window after a resolved map with no admission path, breaking
  refactor-while-green and the non-behavioral return edge, while the PR's own
  integration test asserted reopening. Fixed in P1 (window reopens; closure
  test inverted). run.sh's set -e also HID 15+2 further stragglers behind the
  first failing file: the legacy-contract suites (`test_tdd_summary`,
  `test_repoforge_workflow`) recorded mapped preflights while testing free-form
  semantics — their fixtures now commit map-less pre-Behavior-Map documents as a setup
  shortcut producing the imported-legacy shape (the real importer path is
  proven separately by LegacyImportFreeFormTests), keeping them honest
  legacy-path coverage; and the #139 advisor suite's two `build_document`
  call sites were adapted to the new required-map signature (cross-PR
  integration break caught here).
- Continuation loop independently verified end-to-end at d6cddae (rig drive:
  RED A -> GREEN A -> reassess adds B -> B RED executes valid -> GREEN B ->
  reassess -> tdd passed). Counterfeit-RED rejected two counterfeits while
  accepting the genuine RED.
- Deletion-test verdict: `tdd_workflow.py` (+588) imports seven privates from
  `workflow_cli.py` and its mapped path reimplements `_tdd`'s skeleton inline
  (drift :319, `_run` :333, validity, `_run_entry`); two implementations of one
  behavior. `behavior_map.py` (+219) and the `tdd_surface.evaluate_red`
  discriminator are correctly shaped and stay.

## Delivery map (sequential, stack depth 1, no parallel branches)

| Pass | Vehicle | Owns | Est. net | Gate |
|---|---|---|---|---|
| 1 | PR #138 branch | merge main in; migrate the 4 straggler tests; CI green | ~+40 tests | full suite + CI + install + live governed use |
| 2 | PR #138 branch | absorption refactor: one owner for mapped+legacy TDD (shape decided in-pass via improve-codebase-architecture; monolith question answered with caller evidence) | negative runtime | suite + rig continuation drive + install + live governed use + typed gate |
| 3 | new PR C | sourceRefs consuming PRES-n/ASSUMP-n + design/advisor finding reconciliation into the map | ~+300 | TDD + suite + install + live |
| 4 | new PR D | stage-aware: behavioral `fixed` disposition must resolve mapped GREEN proof via the ledger walk | ~+200 | TDD + suite + install + live |

#138 total is inherited oversize (+2725/-359; runtime +1043 over the 1,000
split threshold). Recorded exception: operator granted ownership with
"proceed immediately"; pass 2 reduces runtime concentration rather than adding;
splitting an open reviewed PR now would manufacture rebase debt the
delivery-governance skill exists to avoid.

Commit structure: pass 1 = one merge commit + one test-migration commit;
pass 2 = one refactor commit (plus mechanical follow-ups if review demands);
passes 3-4 = small conventional commits per feature.

Consolidation trigger: if pass 2's absorption forces rework of >2 of #138's
test files beyond import-path changes, stop and regroup with the operator
before touching more.

## Install-verified testing doctrine (operator-directed, every pass)

Each pass ends by installing the changed `hooks/` + `skills/` paths into
`~/.claude` (after the checkout suite is green, with a dated rollback copy
taken first), and the NEXT pass's own governed workflow — begin, gates,
recorders, TDD, hooks — runs live on that installed machinery. The estate
governing its own development on the code under test is the end-to-end proof;
scratch-rig runs are diagnostics, never the production claim.

## Affected surface

Changed boundary: the workflow TDD state machine (candidate identity, drift,
RED/GREEN validity, map lifecycle) and its recorders. Adjacent consumers:
PreToolUse/PostToolUse edit gating (`rcf-intake-gate.py`,
`post-edit-blast-radius.py`), `workflow complete` reconciliation, the tdd shim
scripts, the codex-advisor wrapper's TDD-evidence attachment (reads
`tddEvidence` through `workflow status` — must keep shape). No-change surfaces:
every non-TDD workflow verb, ledger schema, advisor machinery, #139's
bounded-evidence channels.

Transaction-sensitive (pass 2+): authoritative records = the SQLite event
ledger's TDD evidence chain and active projection; mutation boundary =
`commit_tdd`; interleavings = concurrent recorder calls serialized by the state
lock; replay/recovery = projection repair from ledger history; no-op paths =
refused transitions mutate nothing. Invariant: every accepted TDD transition
commits its evidence document in the same transaction; proof = the existing
ledger/lifecycle suites plus the rig continuation drive.

## Execution checklist

- [x] P1: merge main into the branch (merge tree == unique clean-merge oid 5b31af9c)
- [x] P1: migrate straggler tests (two hook tests to mapped fixtures; policy-gate test inverted to pin reopening; legacy suites to map-less library-committed preflights; advisor suite to the new fixture signature) + fix the 0872eff window over-closure (RED evidence-e86dbe2f / GREEN evidence-c4193900)
- [~] P1: full checkout suite green (141/0 recorded, evidence-ab97f9c2); push + CI pending
- [~] P1: installed (29 files, rollback at ~/.claude/rollback-2026-08-19-p1); installed suite 141/0; live verification = this pass's own remaining chain on the installed CLI/hooks
- [x] P2: improve-codebase-architecture pass -> candidate B recorded (one TDD domain Module; monolith rejected on measured caller evidence: _run/_run_entry are two-consumer utilities -> command_runner.py; TDD-only internals move; hooks keep policy imports; dispatch inverts to workflow_cli.main). Full decision: scratchpad p2-design.md, carried into the pass preflight
- [x] SPEC-1 obligation RESOLVED IN P1 (advisor escalated on the live install): post-resolution production edits stay admitted, but the PostToolUse hook flags the resolved map (annotate_tdd_evidence - a phase-preserving evidence write) so COMPLETION demands one recorded tdd-map reassessment: the behavioral item, or the recorded why-non-behavioral. Batch edits cost one record; the records-why edge is mechanical
- [x] P2: absorption implemented (dual skeleton deleted: _run_tdd is the one lifecycle with the legacy items-None policy branch; command_runner narrowed to run/run_entry/print_output/mute_stdout (emitters later consolidated: pass 6's operator directive made command_runner.emit_json the one owner, superseding the CLI-local-emitters advisor round); workflow_cli 724->~500 lines; one entry for all 7 shims; complete's help restored to main's argparse surface with top-level help byte-identical to 0570b9a). Suite 141/0 before and after each round (evidence-fc6cc4af latest); CI-pinned ruff E9,F clean; preflight refreshed with PRES_COMPLETE_CLI (evidence-8e6f35d7); three final-advisor rounds fixed, round-4 disposition pending
- [x] P2: installed (34 paths reinstalled after every fix round); installed suite 141/0; live governed use = this pass's own recorded chain ran on the installed CLI/hooks throughout
- [~] P2: pushed as ec00ec8; CI contracts PASS; reviewer loop: pass 3 (pr138-p3-reviewfix) closed all 28 threads - 6 demonstrated evaluator/validator defects fixed with mapped RED->GREEN (capture escape incl. exact/self-name + dup-leaf multiplicity, unittest final records, terminal-counts provenance incl. atexit tails, denylist fragments, plus CLI-seam denylist proof), map-input-guard measured already-fail-closed, test/CI hygiene (which-guard, alias imports, pytest install placement), 6 docs corrections, 815-run corpus replay zero validity regressions, 6 final-advisor rounds ending commit-ready; rejected-with-measurement: evidence_document double-unwrap (real-seam refutation), suite-failure claims (141/0+CI), intake-gate TOCTOU (continuity-not-authorization), command_runner full-read (pre-existing); merge when operator confirms. Pass 4 (pr138-p4-nosummary): CodeRabbit's summary-suppression finding on a98738a - premise corrected (-rN/-rs are the real shapes, --no-summary refuses for marker absence), counterfeit acceptance under -rN reproduced, fallback deleted with fail-closed rerun guidance; exact-predicate corpus 0/291, post-fix replay 823 runs zero flips; two final-advisor rounds ending commit-ready. Pass 5 (pr138-p5-contlines): Fleet's seven mediums on 8b56b53 - continuation-line marker refusals (unittest assertEqual long-diff, pytest multiline messages) fixed with structural windows; collapsed-substring denylist replaced with word-sequence matching (advisor-demonstrated USERNAME_ERROR_VISIBLE occurrence overturned the initial rejection); SKILL.md joint-marker sentence and WORKFLOW-MAP tdd-map edge added; opaque-inspection-state and completion-TOCTOU demands rejected with measurements; three final-advisor rounds ending commit-ready. Docs round (2e3f5ce): Fleet's six mediums on 54e9137 - three one-line doc transcriptions of measured behavior (post-resolution why-only path, already-satisfied completion criteria, refactor-while-GREEN admission), three report-only rejections with measurements (disabled-latch recovery nuance, pre-existing interleaved-fixture denylist gap with RED-time enforcement, verbatim-from-main timeout edge). Reviewer loop CLOSED: 41 threads dispositioned and resolved across five rounds, contracts+CodeRabbit green on 2e3f5ce, Fleet's final run posted nothing, zero unresolved threads, MERGEABLE CLEAN - merge remains the operator's call. Pass 6 (pr138-p6-operator, operator findings): tdd --help regression vs main fixed - settled help contract: bare `workflow tdd --help`/-h serves one deterministic identity-free argparse usage (mapped surface + imported-legacy flag epilog), help tokens after -- stay runner-owned, every subcommand pinned exit-0; emit_json consolidated into command_runner (one owner, operator override of the pass-2 CLI-local round); BUDGET OVERRUN RECORDED: +1,244 production/+1,903 tests/+197 docs = +3,344 net vs the ~500 target and 1,000 split threshold - the next umbrella of this scale is split into governed sub-PRs BEFORE coding, per the delivery-governance split rule. Pass 7 (pr138-p7-notrequired, Fleet round on 82e5ea7): the --not-required flag-erasure HIGH reproduced through the real hook+CLI and fixed with the flag guard ordered before the disposition check (both post-edit and post-GREEN diagnostics pinned); rig pytest-env sanitized; WORKFLOW-MAP evidence-truth clause; three TOCTOU/timeout findings rejected with fresh measurements (complete()-snapshot recorded report-only with its settling measurement named)
- [ ] P3: PR C built on merged main; TDD; install; live; reviewer loop
- [ ] P4: PR D likewise
- [ ] Program: re-sync installed estate to merged main after each merge

## Adjudicated decisions (reviewer round, 2026-08-19 late)

- **JSON-emission owner: command_runner.emit_json — SETTLED.** History: pass 2's final
  advisor demanded CLI-local emitters ("command execution is not the right owner; widens
  a shallow Interface"); the operator's one-owner directive reversed that in pass 6; the
  reviewer session then flagged the reversal as unadjudicated. Adjudication on measured
  grounds: command_runner already owned two of the three legs of the reporting-failure
  policy (print_output and mute_stdout - report to stdout, OSError means the mutation is
  already committed and must not be re-labelled a refusal) BEFORE emit_json arrived, so
  ownership follows the policy family, not "command execution"; the pass-6 final advisor
  measured the deletion test in its favor (deleting the Module redistributes
  execution-and-reporting policy across both callers; dependency direction acyclic); the
  Interface widening is deliberate and named in the module docstring. CLI-local loses on
  three-copy drift (operator-rejected); a separate cli_reporting module loses on
  splitting one small real owner into two smaller ones for zero leverage. Do not flip
  this again without new measurements.
- **tdd --help surface: FROZEN at eb34212.** The reviewer withdrew its SPEC-1 as
  report-only after measuring occurrence: 3 hits across 1,155 session transcripts, all
  probes (vs 14 real invocations for the top-level --help defect, which is fixed). The
  shipped identity-free help stays (green, installed, unwinding costs more than it
  saves), and the surface is closed to further hardening: any future --help token edge
  case is dispositioned report-only citing this measurement, not a behavior-map item.
- **Budget overrun handling (operator-constrained).** Net moved +3,344 -> +3,524
  addressing the reviewer round. Verbatim constraint: never reduce the number by
  stripping working code that would impact behavior or performance; the only routes are
  stop adding, split remaining scope to follow-ups, or record the overrun with reason.
  Applied: the --help surface is frozen (above), _legacy_parser retirement is a filed
  follow-up issue, and the overrun stands recorded.
- **_legacy_parser retirement: follow-up issue, not this PR.** Measured load-bearing:
  13 real in-flight passes across 6 repos route legacy today (56 more stored passes are
  disposable probe/arm dirs). Keep it; the missing piece is the retirement event and its
  checker, recorded in the follow-up issue.

## Self-critique (recorded)

Merge-vs-rebase: the lead said "rebase"; a 69-commit replay against a branch
whose endpoint merges clean invites intermediate conflicts for zero content
difference. Merge commit chosen; endpoint identical; noted here as the one
deviation from the lead's wording, on delivery-governance's own anti-rebase-debt
rule. PR-count critique: pass 2 inside #138 rather than a separate refactor PR
— because the adapter is #138's own debt and production-code forbids landing it
unnamed; a separate PR would review the same lines twice. Owner-slice check:
every behavior above names exactly one pass. Verification: each pass carries
its own gate; nothing merges on a red suite.
