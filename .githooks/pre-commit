#!/usr/bin/env bash
set -euo pipefail

hook_dir=$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)
exec python3 "$hook_dir/../skills/codex-advisor/scripts/commit-approval.py" check --cwd "$PWD"
